class Pricing:
  private val base = 1
  private val other = 2
  def first(a: Int): Int = a + base
  def second(b: Int): Int = b + base
  def alt(y: Int): Int = y + base
  def otherLine(b: Int): Int = b + other
